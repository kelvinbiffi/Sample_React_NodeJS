name: Sample_React_NodeJS
packages:
  - path: src
language: javascript
appmap_dir: tmp/appmap
test_recording:
  test_commands:
    - command: npx @appland/appmap-agent-js --recorder=process -- npm run test
      env: {}
